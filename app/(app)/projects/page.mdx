import { defaultMetadata } from "@/app/_default-metadata";
import { ListProjects } from "../_components/list-projects";

export const metadata = { ...defaultMetadata, alternates: { canonical: "/projects" } };

# Projects

<ListProjects />