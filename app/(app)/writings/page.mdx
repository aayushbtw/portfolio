import { defaultMetadata } from "@/app/_default-metadata";
import { ListPosts } from "../_components/list-posts";

export const metadata = { ...defaultMetadata, alternates: { canonical: "/writings" } };

# Writings

<ListPosts />